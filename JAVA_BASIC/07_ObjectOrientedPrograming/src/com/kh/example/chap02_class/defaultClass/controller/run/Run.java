package com.kh.example.chap02_class.defaultClass.controller.run;

//import com.kh.example.chap02_class.defaultClass.controller.DefaultClassTest;

public class Run {
	public static void main(String[] args) {
		//DefaultClassTest dct = new DefaultClassTest();
		
		// DefaultClassTest cannot be resolved to a type
		// defualt 클래스는 같은 패키지안에서만 접근이 가능.
		
		// default 클래스는  외부 패키지  에서 접근이 불가능하기 때문에 없는 클래스라는 에러 발생
		// 하지만 같은 패키지 내에서는 사용 가능.
		
	
	//	dct.test();
		
		
	}
}
