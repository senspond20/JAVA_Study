다형성 : ' 여러 개의 형태를 갖는다 '
하나의 행동으로 여라가지 일을 수행하는 개념
상속을 이용해 부모 타입으로부터 파생된 여러 가지 타입의 자식 객체를 부모 클래스 
타입 하나로 다룰 수 있는 기술

타입 -> 자료형
클래스도 자료형

부모 클래스로부터 파생된 여러가지 타입의 자식객체
(부모 객체를 상속받고 있는 여러개의 자식)

를 부모 클래스 타입 하나로 다룰 수 있는 기술.


원 도형 삼각형

//
instanceof 연산자.
현재 참조형 변수가 어떤 클래스 형의 객체 주소를 참조하고 있는지 확인 할
때 사용하는 연산자로 클래스 타입이 맞으면 true, 맞지 않으면 false 반환

바인딩

오버라이딩 -> 
자식 객체를 통한 실행 시 후손 것이 우선권을 가짐

컴파일러가 실행될때 

처음에는 

추상클래스---> 그 자체로 객체로 만들 수 없다.
=> 상속 강제. 반드시 상속하여 객체 생성

동적 바인딩 (어디에 묶일거나)


추상 클래스
1. 미완성 클래스(abstract 키워드 사용)
2. 
3. 클래스 내에 일반변수, 메소드 포함 가능
4. 객체 생성은 안되지만 참조형 변수 타입으로는 사용 가능.

인터페이스
->다중 상속

static 으로 올라간다.

# protected
기울여져 있으면 abstract
